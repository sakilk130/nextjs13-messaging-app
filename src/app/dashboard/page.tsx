import Button from '@/components/ui/Button';
import { FC } from 'react';

interface pageProps {}

const page: FC = () => {
  return (
    <Button variant="default" size={'sm'}>
      Hello
    </Button>
  );
};

export default page;
